# 🚀 量化交易平台 - 快速参考

## 📍 平台地址

**http://127.0.0.1:8108**

## 📊 当前状态

- ✅ 系统运行中
- 💰 总资金: 1,000,000
- 💵 可用现金: 1,000,000
- 📈 策略数量: 3个
- 📋 持仓: 空
- 📝 订单: 无

---

## 🎯 常用API命令

### 查看类
```bash
# Dashboard总览
curl http://127.0.0.1:8108/api/dashboard | jq '.'

# 策略列表
curl http://127.0.0.1:8108/api/strategies | jq '.[] | .name'

# 订单历史
curl http://127.0.0.1:8108/api/orders | jq '.'
```

### 操作类
```bash
# 创建策略 (示例: 黄金双均线)
curl -X POST http://127.0.0.1:8108/api/strategies \
  -H "Content-Type: application/json" \
  -d '{
    "name": "黄金双均线",
    "dataset": "gold_daily",
    "spec": {
      "symbol": "XAUUSD",
      "indicators": [
        {"name": "fast", "type": "sma", "window": 12},
        {"name": "slow", "type": "sma", "window": 36}
      ],
      "entry_rules": [
        {"left": "fast", "op": "crosses_above", "right": "slow"}
      ],
      "exit_rules": [
        {"left": "fast", "op": "crosses_below", "right": "slow"}
      ],
      "position_sizing": {"mode": "fixed_fraction", "risk_fraction": 0.1},
      "risk_limits": {"stop_loss_pct": 0.05, "take_profit_pct": 0.12}
    }
  }'

# 运行回测
curl -X POST http://127.0.0.1:8108/api/backtest \
  -H "Content-Type: application/json" \
  -d '{
    "strategy_id": "YOUR_STRATEGY_ID",
    "start_date": "2023-01-01",
    "end_date": "2023-12-31"
  }'

# 执行交易
curl -X POST http://127.0.0.1:8108/api/execute \
  -H "Content-Type: application/json" \
  -d '{
    "strategy_id": "YOUR_STRATEGY_ID",
    "broker_mode": "paper"
  }'
```

---

## 📦 内置数据集

| 数据集 | 说明 | 使用示例 |
|--------|------|----------|
| `gold_daily` | 黄金日线 | `"dataset": "gold_daily"` |
| `copper_daily` | 铜日线 | `"dataset": "copper_daily"` |
| `ndx_daily` | 纳指日线 | `"dataset": "ndx_daily"` |

---

## 🎨 支持的指标

- `sma` - 简单移动平均
- `ema` - 指数移动平均
- `rsi` - 相对强弱指标
- `macd` - MACD
- `momentum` - 动量
- `bollinger_bands` - 布林带
- `atr` - 平均真实波幅

---

## ➗ 支持的操作符

- `>` / `<` / `>=` / `<=` - 比较
- `crosses_above` - 向上穿越
- `crosses_below` - 向下穿越

---

## 🎓 核心功能模块

### 1. 欧奈尔CANSLIM系统
```bash
# 运行示例
python3 examples/oneill_strategy_example.py

# 港股演示
python3 examples/hk_stock_demo.py
```

### 2. MAE/MFE优化
```bash
# 运行示例
python3 examples/mae_mfe_example.py
```

### 3. 因果AI分析
```bash
# 因果状态
curl http://127.0.0.1:8108/api/causal/status

# 因果决策
curl http://127.0.0.1:8108/api/causal/decision?symbol=XAUUSD
```

---

## 📚 完整文档

- **使用指南**: `平台使用指南.md`
- **API文档**: `API_DOCUMENTATION.md`
- **欧奈尔系统**: `docs/欧奈尔交易体系指南.md`
- **MAE/MFE系统**: `docs/MAE_MFE_优化系统指南.md`

---

## 💡 典型工作流程

```
1. 访问前端
   ↓
2. 创建/编辑策略
   ↓
3. 运行回测验证
   ↓
4. 查看回测结果
   ↓
5. 执行Paper交易
   ↓
6. 监控持仓
   ↓
7. 调整优化
```

---

**系统版本**: v1.0
**更新时间**: 2026-05-02
**GitHub**: https://github.com/pamelacai310-sketch/quant-trading-system
